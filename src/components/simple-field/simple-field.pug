mixin simple-field(labelTitle, inputType, fieldName, formId, fieldValue)
    div(class="profile__fields")
        label(class="profile__field") #{labelTitle}
        input(class="profile__field-value" type=inputType, name=fieldName, form=formId value=fieldValue) 
